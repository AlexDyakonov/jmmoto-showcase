
# Telegram Mini App Template (Русский)

Полнофункциональный шаблон для создания Telegram Mini Apps с бэкендом на Go и фронтендом на React.

[Read in English](README.md)

## 🚀 Возможности

- **Бэкенд**: Go с REST API фреймворком Huma
- **Фронтенд**: React 19 + Vite + TypeScript + Tailwind CSS
- **База данных**: PostgreSQL с миграциями
- **Интеграция с Telegram**: Bot SDK и поддержка Mini App
- **Документация API**: Автоматически генерируемая документация OpenAPI/Swagger
- **Docker**: Docker Compose для удобной разработки

## 📋 Требования

- Go 1.24+
- Node.js 18+ и npm
- Docker и Docker Compose
- PostgreSQL 16+ (или используйте Docker Compose)

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd tma-template
```

### 2. Настройка бэкенда

Перейдите в директорию server:

```bash
cd server
```

Создайте файл `.env` в директории `server`:

```env
# Конфигурация сервера
HTTP_PORT=8000
HTTP_HOST=0.0.0.0
DEBUG=true

# Конфигурация базы данных
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=cats

# Конфигурация Telegram
TG_BOT_TOKEN=ваш_токен_бота
WEBAPP_NAME=имя_вашего_веб_приложения

# Конфигурация хранилища (опционально)
STORAGE_IMAGES_PATH=images

# Конфигурация S3 (опционально)
S3_ACCESS_KEY_ID=
S3_SECRET_KEY=
S3_REGION=
S3_BUCKET=
S3_ENDPOINT_URL=
S3_ROOT_DIRECTORY=

# Логирование
LOG_HANDLER=tint
```

Установите зависимости Go:

```bash
go mod download
```

### 3. Настройка фронтенда

Перейдите в директорию client:

```bash
cd ../client
```

Установите зависимости:

```bash
npm install
```

Обновите `public/config.js` с вашей конфигурацией:

```javascript
window.api = {
  BOT_USERNAME: "ваш_username_бота",
  API_URL: "http://localhost:8000",
};
```

## 🏃 Запуск приложения

### Вариант 1: Использование Docker Compose (Рекомендуется для базы данных)

Запустите базу данных и Adminer:

```bash
cd server
make db-compose-up
```

Запустите миграции базы данных:

```bash
make db-migrate-up
```

Запустите бэкенд сервер:

```bash
make server-run
```

В другом терминале запустите фронтенд:

```bash
cd client
npm run dev
```

### Вариант 2: Полный Docker Compose

Запустите все сервисы:

```bash
cd server
make compose-up
```

### Вариант 3: Ручная настройка

1. Запустите PostgreSQL базу данных
2. Запустите миграции: `make db-migrate-up`
3. Запустите бэкенд: `make server-run`
4. Запустите фронтенд: `cd client && npm run dev`

## 📚 Документация API

После запуска сервера доступна документация API:

- **OpenAPI YAML**: http://localhost:8000/api/v1/openapi.yaml
- **OpenAPI JSON**: http://localhost:8000/api/v1/openapi.json
- **Интерактивная документация**: http://localhost:8000/api/v1/docs

## 🗄️ Миграции базы данных

### Применить миграции

```bash
cd server
make db-migrate-up
```

### Откатить миграции

```bash
make db-migrate-down
```

## 🧪 Разработка

### Команды бэкенда

```bash
cd server

# Запустить сервер
make server-run

# Запустить Telegram бота
make bot-run

# Проверить код линтером
make lint

# Исправить проблемы линтера
make lint-fix

# Сгенерировать документацию Swagger
make swag
```

### Команды фронтенда

```bash
cd client

# Запустить сервер разработки
npm run dev

# Собрать для продакшена
npm run build

# Предпросмотр продакшен сборки
npm run preview

# Проверить код линтером
npm run lint

# Исправить проблемы линтера
npm run lint-fix
```

## 🔧 Конфигурация

### Переменные окружения

См. пример `.env` выше для всех доступных опций конфигурации.

### Конфигурация фронтенда

Отредактируйте `client/public/config.js` для настройки:
- Username бота
- URL API

## 🐳 Docker

### Собрать и запустить с Docker Compose

```bash
cd server
docker compose up --build
```

### Остановить сервисы

```bash
docker compose down
```
